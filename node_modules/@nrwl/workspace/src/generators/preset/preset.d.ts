import { Tree } from '@nrwl/devkit';
import { Schema } from './schema';
export declare function presetGenerator(tree: Tree, options: Schema): Promise<() => Promise<void>>;
export default presetGenerator;
